# Intent service deps — the model + classification stack only (CPU). The Docker
# build installs CPU-only torch from the PyTorch CPU index first; sentence-
# transformers then resolves against it. No media/storage deps here. pydantic
# comes in via fastapi.
fastapi
uvicorn
sentence-transformers
scikit-learn
numpy
joblib
loguru
